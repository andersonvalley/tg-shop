import { Wrapper } from '@/src/components/UI/layout/wrapper'
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Рассылки',
}

export default function Share() {
  return <Wrapper title="Рассылки">hello</Wrapper>
}
